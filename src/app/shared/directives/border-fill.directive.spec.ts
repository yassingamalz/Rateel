import { BorderFillDirective } from './border-fill.directive';

describe('BorderFillDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderFillDirective();
    expect(directive).toBeTruthy();
  });
});
